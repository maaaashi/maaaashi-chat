type Channel {
  id: String!
  name: String!
}

type Chat {
  id: String!
  userName: String!
  content: String!
  channelId: String!
  createdAt: String!
}

type Query {
  listData(type: String!, createdAt: String!): [Channel!]!
  listChannelChats(channelId: String!): [Chat!]!
}
