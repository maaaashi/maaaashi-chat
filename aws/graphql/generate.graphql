schema {
  query: Query
}

type DynamoDBData {
  createdAt: String!
  pk: String!
  sk: String!
  value: String!
}

type Query {
  listData(createdAt: String!, type: String!): [DynamoDBData!]!
  listTypeData(pk: String!, type: String!): [DynamoDBData!]!
}