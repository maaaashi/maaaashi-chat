schema {
  query: Query
}

type DynamoDBData {
  createdAt: String!
  pk: String!
  sk: String!
  value: String!
}

type Query {
  listChannelChats(channelId: String!): [DynamoDBData!]!
  listData(createdAt: String!, type: String!): [DynamoDBData!]!
}